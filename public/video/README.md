Case-study clips for the "WebView vs native" section, using these exact filenames:

- `sun.mp4` — Android Studio walkthrough. Present.
- `ios.mp4` — Safari Web Inspector walkthrough. Still needed.

Vite serves everything in `public/` as-is from the site root, so once `ios.mp4` is added it'll be reachable at `/video/ios.mp4` with no further code changes.
